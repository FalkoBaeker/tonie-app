ENVIRONMENT=staging
DEBUG=false
PORT=8787

# Auth mode
AUTH_MODE=external
AUTH_REQUIRE_VERIFIED_EMAIL=true
AUTH_JWKS_URL=
AUTH_ISSUER=
AUTH_AUDIENCE=

# Keep sqlite for staging quickstart unless cloud postgres migration is done
SQLITE_PATH=./tonie_finder.db
